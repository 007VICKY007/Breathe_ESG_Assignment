# Railway WEB service only — set Root Directory to "backend" in service settings
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "chmod +x start.sh && ./start.sh"
healthcheckPath = "/api/v1/health/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
